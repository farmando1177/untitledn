buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // أحدث إصدار متاح
        classpath 'com.google.gms:google-services:4.4.2' // النسخة الأحدث من Google Services
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.22' // إذا كنت تستخدم Kotlin

    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}